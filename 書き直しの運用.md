# Terra による記事の書き直し（運用の決めごと）

**作成: 2026-09-10。同日夕方に最終決定。**

## 決まったこと（運営者の指示）
- **めざめ46本＋スパイラル10本を、すべて Terra で書き直してサイトに反映する。** いちいち確認しない。
- **元の記事は消さない。** `元の記事バックアップ/mezame/` と `元の記事バックアップ/spiral/` に置き換え前の全文がある。
- くらべファイルは作らない。検査に通ったものはそのまま置き換えて公開。
- 怪異と謎（content/）は「完結・凍らせておく」と以前に決めているので対象外。やるなら運営者に聞く。
- **Luna は使わない**（長い記事で引用を落とす）。**Sol も使わない（高い）。**
- モデルを変える・お金のかかる試しをする・生成物を消す、は必ず先に聞く。

## 仕組み
| 役 | 誰 | 何をするか |
|---|---|---|
| 書き手 | GPT-5.6 **Terra**（`ai.config` の `POLISH_MODEL`） | 下書きを素材に自分の言葉で書き直す（`polish.py` 既定の自由モード。「もう少しだけ自由に」の指示ずみ） |
| 検査 | `scripts/polish.py` の `check()` | 8文字以上の引用が消えた／元に無い引用を作った／リンクの増減／禁止語／太字増／極端な分量・見出し数 → 落とす。落ちたら1回だけ自動でやり直し |
| 一括 | `scripts/rewrite_all.py` | 1本ずつ `polish.py --apply`。通ったら置き換え、2回落ちたら元のまま。進み具合は `_書き直しログ.txt`。途中から再開できる |

## Terra に渡すもの
本文（HTML）＋頭にある要約と出典の一覧（参考資料。本文に足させない・リンクにさせない）＋引用文の一覧（1文字も変えるな）。

## 手順
```
python scripts/rewrite_all.py     全部回す（済んだ分は飛ばす）
python scripts/build.py           ページを作り直す。リンク切れ 0 を確認
git add -A && git commit && git push
```
1本だけなら `python scripts/polish.py mezame/xxx.txt --apply`。`--strict` で「なめらかにするだけ」。

## 費用のめやす
Terra で1本 5〜10円。全56本で 500円前後。
